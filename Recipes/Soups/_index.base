views:
  - type: cards
    name: View
    filters:
      and:
        - file.tags.contains("soup")
    order:
      - file.name
      - level
      - totalTime
    sort:
      - property: file.name
        direction: DESC
    image: note.cover
